---
doc_id: BND-L34-001
level: contract
layer: cross-layer
component: null
status: candidate
baseline: AKB-2026-09-03-09
authoritative_for: "边界契约、公共元数据或契约语义的唯一来源"
parent: SYS-DES-001
interfaces: [BND-L34-001]
diagrams: []
supersedes: [docs/design/c4-boundary-protocols.md]
---

# BND-L34-001：L3 Tool Runtime ↔ L4 Execution Runtime

唯一入口 `SandboxPort` 表达一次性、有界执行：创建隔离环境、执行、返回规范化结果并回收。具体请求/响应签名待步骤二确认。远程业务 Tool Provider 使用独立 `ToolProviderPort`，不得复用 Sandbox Port。

首版本地子进程使用 stdout 纯净的 JSONL，stderr 只写脱敏日志；同进程只读 Adapter 仍受路径、权限和结果大小限制，且无 Shell 能力。初始化协商版本、能力和最大帧；输入、输出、时间、进程、挂载与出口均有上限。失败不得回退为宿主执行。
