# FlowEngine 测试评审复核

结论：**本轮测试设计评审通过**。原 3 项 P2 均已在当前文档与设计向量中修正；本次定向复核未发现新增 P1/P2。此结论不代表 FlowEngine 实现、真实持久化恢复或故障注入测试已经执行通过。

| 原问题 | 当前证据 | 复核结果 |
|---|---|---|
| 取消验收要求所有外部动作必然结束 | `flow-engine.md:1732`、`:1774` 明确停止新派发并保存可追踪事实；`:1865` 添加不响应取消的工具/Child、30 秒窗口、本地资源释放与耐久 incident 断言 | 已解决；可撤销与不可撤销执行的验收边界一致 |
| JSON 字符串编码不是唯一字节形式 | `flow-engine.md:2376-2378` 明确 ECMAScript JSON.stringify 字符串规则、控制字符转义、UTF-8、不转义斜线及非法代理项拒绝；examples JSON `:4-55` 共 6 个向量，覆盖控制字符/斜线/U+2028/U+2029、非 BMP 键排序及不做 Unicode 归一化 | 已解决；原跨实现摘要歧义关闭 |
| absent 后旧提交迟完成缺少故障验收 | `flow-engine.md:1856-1864` 要求旧提交优先与 claim 失效优先两个屏障分支，首次 absent 后零重算/派发，并要求真实持久化 Adapter 共用契约 | 已解决；成功丢 ACK 与尚未提交的超时得到独立验证设计 |

路径基准：`/Users/oscar/jurismind/code/pi-lawclaw/packages/lawclaw-agent-kernel/docs/design/layers/l1-control/components/`。行号对应本次复核时工作区版本。

验证证据：使用独立临时 Node 脚本重算 6 个规范化字节串/SHA-256 固定值，并按当前 21.5（包含 contextFailure）重算 V1—V8 适用的事件、输入、决策及命令摘要，均匹配。未修改仓库文件或生产代码；临时脚本执行后删除。

尚需实施后验收：原有测试清单及新增 12.5.6a 的故障脚本、真实进程崩溃/RunRegistry Adapter 测试、取消隔离、性能及容量测试。它们属于未执行的实现验证，不影响本次对三项设计修正的通过判断。不得据本报告勾选运行测试或生产上线门禁。
